# Tasks

- [x] Add `zod` to `dependencies` in `package.json`.
- [x] Replace `catch (err: any)` in tool handlers with `unknown` guards.
- [x] Add typed interfaces for parsed PowerShell JSON payloads.
- [x] Remove `any` usage from `src/powershell.ts` error checks.
